'use strict';

/**
 * @ngdoc function
 * @name realrig.controller:
 * @description
 * # AboutCtrl
 * Controller of the realrigApp
 */
angular.module('realrig')
  .controller('LoginController', function ($scope) {
    $scope.submit = function(){
      $scope
    }
  });
